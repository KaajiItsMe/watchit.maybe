{
  "name": "watchit-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker stream-proxy for WatchIt.maybe (inject headers + CORS + HLS/DASH manifest rewrite).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
